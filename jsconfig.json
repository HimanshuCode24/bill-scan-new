{
  "compilerOptions": {
    "baseUrl": ".", // This ensures that the paths are relative to the root of the project
    "paths": {
      "@/*": ["src/*"]  // Alias '@' to point to the 'src' directory
    }
  },
  "include": ["src", "pages"] // Include your source directory
}
